name: SAD Manual CI

on:
  workflow_dispatch:  # ⬅️ Дозволяє запуск вручну

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 🔄 Клонування репозиторію
      uses: actions/checkout@v3

    - name: 🐍 Встановлення Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: 📦 Встановлення залежностей
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: ✅ Перевірка запуску Streamlit
      run: |
        streamlit run sad_app.py --server.headless true &
        sleep 10
        curl -I http://localhost:8501 || echo "Streamlit не відповідає"
